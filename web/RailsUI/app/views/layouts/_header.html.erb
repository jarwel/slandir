<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <div class="brand">Slandir</div>
      <div class="nav-collapse">
        <ul id="navigation-bar" class="nav">
          <li><%= link_to "Home", search_home_path %></li>
          <li><a href="#about">About</a></li>
          <li><a href="#contact">Contact</a></li>
          <% if !current_account.nil? %>
            <li><%= link_to "Manage", manage_index_path %></li>
          <% end %>
        </ul>
        <% if current_account.nil? %>
          <%= render :partial => 'layouts/login' %>
        <% else %>
          <p class="navbar-text pull-right"><%= current_account.email %> | <%= link_to "logout", login_destroy_path, :method => :delete %></p>
        <% end %>
      </div>
    </div>
  </div>
</div>