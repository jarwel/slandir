<div class="container">
  <div class="hero-unit">
    <h2>Manage Grievances</h2>
    <p>Removing a grievance will hide it from public view. This change can not be undone.</p>
  </div>
  <% @grievances.each do |grievance| %>
    <div class="row">
      <div class="span1 offset1"><%= link_to "Remove", manage_destroy_path(:grievanceId => grievance.id), :class => "btn", :method => :delete %></div>
      <div class="span5"><h3>Posted as <%= grievance.author %> in <%= grievance.created.strftime("%B %Y") %></h3></div>
    </div>
    <div class="row">
      <div class="span10 offset1 well"><%= grievance.description %></div>
    </div>
  <% end %>
</div>